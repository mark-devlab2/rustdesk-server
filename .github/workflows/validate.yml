name: Validate Aliyun Release Contract

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    uses: mark-devlab2/aliyun-deploy-platform/.github/workflows/validate-service.yml@codex/rustdesk-platform-deploy
    with:
      build_config_path: .deploy/build.yaml
      platform_repo: mark-devlab2/aliyun-deploy-platform
      platform_ref: codex/rustdesk-platform-deploy
